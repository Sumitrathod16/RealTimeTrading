{
  "name": "realtime-trading-platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev:ui": "npm run dev --prefix frontend",
    "dev:api": "dotnet run --project Trading.Api",
    "build:api": "dotnet build Trading.Api.sln"
  }
}
